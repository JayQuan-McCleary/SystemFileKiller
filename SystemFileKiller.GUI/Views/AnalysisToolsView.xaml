<UserControl x:Class="SystemFileKiller.GUI.Views.AnalysisToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Bg}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ScreenHeadBorder}">
            <StackPanel>
                <TextBlock Style="{StaticResource EyebrowText}" Text="MODE · 11"/>
                <TextBlock Style="{StaticResource ScreenTitle}" Text="Analysis Tools"/>
                <TextBlock Foreground="{DynamicResource Fg3}" FontSize="12" Margin="0,4,0,0"
                           Text="File hash + Authenticode signature for VT/Talos lookups; process tree and ancestry chain for &quot;what spawned this?&quot; triage."/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- File hash + signature pane -->
            <Border Grid.Row="0" Background="{DynamicResource Bg2}" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1" Padding="22,14,22,16">
                <StackPanel>
                    <TextBlock Style="{StaticResource PanelSectionTitle}" Margin="0,0,0,8" Text="FILE PATH"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{DynamicResource Panel}" BorderBrush="{DynamicResource Line2}" BorderThickness="1" CornerRadius="2" Padding="10,5">
                            <TextBox x:Name="FilePathBox" BorderThickness="0" Background="Transparent" MinWidth="200"/>
                        </Border>
                        <Button Grid.Column="1" Style="{StaticResource Btn}" Content="Browse…" Click="BrowseFile_Click" Margin="8,0,0,0"/>
                        <Button Grid.Column="2" Style="{StaticResource Btn}" Content="Hash" Click="Hash_Click" Margin="8,0,0,0"/>
                        <Button Grid.Column="3" Style="{StaticResource Btn}" Content="Verify Signature" Click="Sig_Click" Margin="8,0,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource ConsoleBorder}" Margin="22,12">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16,12">
                    <StackPanel>
                        <TextBlock Style="{StaticResource PanelSectionTitle}" Margin="0,0,0,4" Text="HASH"/>
                        <TextBlock x:Name="HashOut" FontFamily="{StaticResource FontMono}" FontSize="11" Foreground="{DynamicResource Fg2}" TextWrapping="Wrap" Text="—"/>
                        <TextBlock Style="{StaticResource PanelSectionTitle}" Margin="0,12,0,4" Text="SIGNATURE"/>
                        <TextBlock x:Name="SigOut" FontFamily="{StaticResource FontMono}" FontSize="11" Foreground="{DynamicResource Fg2}" TextWrapping="Wrap" Text="—"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Process tree pane -->
            <Border Grid.Row="2" Background="{DynamicResource Bg2}" BorderBrush="{DynamicResource Line}" BorderThickness="0,1,0,1" Padding="22,14,22,16">
                <StackPanel>
                    <TextBlock Style="{StaticResource PanelSectionTitle}" Margin="0,0,0,8" Text="PID"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{DynamicResource Panel}" BorderBrush="{DynamicResource Line2}" BorderThickness="1" CornerRadius="2" Padding="10,5">
                            <TextBox x:Name="PidBox" BorderThickness="0" Background="Transparent" MinWidth="80"/>
                        </Border>
                        <Button Grid.Column="1" Style="{StaticResource Btn}" Content="Tree (descendants)" Click="Tree_Click" Margin="8,0,0,0"/>
                        <Button Grid.Column="2" Style="{StaticResource Btn}" Content="Ancestry (parents)" Click="Anc_Click" Margin="8,0,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Grid.Row="3" Style="{StaticResource ConsoleBorder}" Margin="22,12">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16,12">
                    <TextBlock x:Name="TreeOut" FontFamily="{StaticResource FontMono}" FontSize="11" Foreground="{DynamicResource Fg2}" TextWrapping="NoWrap" Text="—"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
